export const BUILDER_CONVERSATIONS_PATH = '/admin/builder-conversations';

/** Mirrors the `admin:*` convention the table screens use for their stored params. */
export const BUILDER_CONVERSATIONS_AGENT_STORAGE_KEY = 'admin:builder-conversations-agent';
