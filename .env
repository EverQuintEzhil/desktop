# Tauri runs on tauri://localhost, so the api/files hosts cannot be derived from
# window.location like the web app does (see src/lib/axios.ts) — set them explicitly.
VITE_API_BASE_URL=https://api.perkinswill.fluentmind.dev
VITE_FILES_BASE_URL=https://files.perkinswill.fluentmind.dev
